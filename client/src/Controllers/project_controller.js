// // const birds_json = require('../public/nzbird.json');
// // const project_model = require("../models/user");

// const { search_string } = require("./project_utils.js");

// // async function filter_project_data(search, status, sort) {
// //   var results = await project_model.find();
// //   // filter by search string
// //   if (search !== undefined && search !== "") {
// //     results = search_string(results, search);
// //   } else {
// //     console.log("Not found");
// //   }

// //   return results;
// // }

// module.exports = { filter_project_data };
